<form method="post" action="{{ path('app_coincoins_delete', {'id': coincoin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coincoin.id) }}">
    <button class="btn">Delete</button>
</form>
