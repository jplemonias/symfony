{% extends 'base.html.twig' %}

{% block title %}Coincoins{% endblock %}

{% block body %}
    <h1>Coincoins</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coincoin.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ coincoin.content }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ coincoin.createdAt ? coincoin.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coincoins_index') }}">back to list</a>

    <a href="{{ path('app_coincoins_edit', {'id': coincoin.id}) }}">edit</a>

    {{ include('coincoins/_delete_form.html.twig') }}
{% endblock %}
